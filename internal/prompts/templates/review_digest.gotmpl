Create a concise review digest for the plan below.

Keep explicit reviewer decisions separate from agent-generated plan text. Include only side-question answers the reviewer promoted.

{{.ProtocolDocumentation}}

Agent-generated plan:
{{.Plan}}

Reviewer decisions:
{{.ReviewerDecisions}}

Promoted side-question answers:
{{.PromotedSideAnswers}}

Model-facing review context (the XML tags are metadata, not plan text):
{{.ReviewContext}}
