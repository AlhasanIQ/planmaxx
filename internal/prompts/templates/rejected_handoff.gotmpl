The user rejected this plan with comments. Address the comments in the rejection digest, then provide a revised plan.

{{.ProtocolDocumentation}}

Rejected plan:

{{.PlanBlock}}
Rejected review digest:

{{.DigestBlock}}
Model-facing review context (the XML tags are metadata, not plan text):

{{.ReviewContext -}}
