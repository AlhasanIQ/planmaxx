PlanMaxx review rejected.

The user rejected this plan with comments. Address the comments in the rejection digest, then reiterate the plan until the user is satisfied.

Rejected plan:

{{.PlanBlock}}
Rejected review digest:

{{.DigestBlock -}}
